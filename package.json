{
  "name": "Mincraft",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Performance-first voxel engine with a high-fidelity rasterized renderer, tuned for Intel UHD 620 class hardware.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
